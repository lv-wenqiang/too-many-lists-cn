# 一个好的单向链表栈

在上一章中，我们写了一个最小可行单向链表
栈。但是，有一些设计决策使其有点糟糕。
让我们让它不那么糟糕。在此过程中，我们将：

* 重复造轮子
* 使我们的列表能够处理任何元素类型
* 添加查看
* 使我们的列表可迭代

在这个过程中，我们将了解

Option 的高级用法
泛型
生命周期
迭代器

让我们添加一个名为`second.rs`的新文件：

```rust ,ignore
// in lib.rs

pub mod first;
pub mod second;
```

并将`first.rs`中的所有内容复制到其中。
